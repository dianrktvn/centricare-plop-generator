import { createMutation } from "../../../lib";
import { create{{pascalCase name}} } from "../api/create{{pascalCase name}}";

export const useCreate{{pascalCase name}} = createMutation(
  create{{pascalCase name}},
  [["{{camelCase name}}"]],
);